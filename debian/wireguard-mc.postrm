#!/bin/sh
set -e

case "$1" in
remove|purge)
	if command -v systemctl >/dev/null 2>&1 && [ -d /run/systemd/system ]; then
		systemctl daemon-reload >/dev/null 2>&1 || true
		systemctl reset-failed 'wireguard-go@*' >/dev/null 2>&1 || true
	fi
	;;
esac

#DEBHELPER#

exit 0
